// /lib/routes.ts

/**
 * All static routes for Macaque
 * Keep this as the single source of truth.
 * Always use these instead of hardcoding paths.
 */
export const routes = [
  "",
  "/build-your-bar",
  "/about-us",
  "/contact-us",
  "/pricing",
];
